# Reference

Implementation-oriented reference docs live here.

Current docs:

- `runtime-contract.md` for frozen run snapshots, state machines, artifact model, storage boundaries, and resume/retry rules

Planned docs:

- workflow schema reference
- agent catalog schema reference
- state transition diagrams
