import SwiftUI

/// Placeholder — the provider setup evidence panel has been removed along
/// with its backing types. This file is retained as an empty stub so the
/// Xcode project reference stays valid.
struct ProviderSetupEvidencePanel: View {
    var body: some View {
        EmptyView()
    }
}
