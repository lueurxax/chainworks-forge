import SwiftUI

enum ForgeStatusColor {
    static let success = ForgeColor.Status.success
    static let warning = ForgeColor.Status.warning
    static let approval = ForgeColor.Status.approval
    static let error = ForgeColor.Status.error
    static let running = ForgeColor.Status.running
    static let neutral = ForgeColor.Status.neutral
    static let cancelled = ForgeColor.Status.cancelled
}
