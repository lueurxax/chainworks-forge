# macOS
.DS_Store

# Xcode user data
*.xcuserstate
*.xcscmblueprint
*.xccheckout
xcuserdata/

# Build products
build/
DerivedData/
DerivedDataCodex/
.xcode-cloud/

# Generated documentation/export bundles
/docs.zip

# Local orchestration scratch reports
/known-issues.md
/auto-retry-blocked-runs-known-issues.md

# Local MCP/JSON-RPC response dumps accidentally produced at repo root
/0
/1

# Engine runtime output channel — never committed; run `git rm --cached CHAINWORKS_OUTPUT` if tracked
/CHAINWORKS_OUTPUT

# Swift Package Manager
.build/
control-plane/target/
control-plane/control-plane/

# Temporary editor files
*.swp
*.swo

# Local tool state
.claude/
.chainworks/
traces/
.forge-codex-acp/
control-plane/.junie/
control-plane/chainworks-control-plane.db
control-plane/chainworks-control-plane.db-*
control-plane/chainworks-control-plane.db.backup-*.sqlite
control-plane/chainworks-control-plane.db.backup-*.sqlite-shm
control-plane/chainworks-control-plane.db.backup-*.sqlite-wal
control-plane/crates/daemon/principals.json

# Local SQLite runtime databases at repository root.
/*.db
/*.db-shm
/*.db-wal
/chainworks-control-plane.db.backup-*.sqlite
/chainworks-control-plane.db.backup-*.sqlite-shm
/chainworks-control-plane.db.backup-*.sqlite-wal

# P042 §10.5 release-host credentials (checked via example template only).
scripts/packaging.env

# Generated Chainworks run-output envelopes — must never be committed.
# These are produced by agent sessions at repo root when the output channel
# is misconfigured; the engine materializes canonical outputs at the run meta-root.
/CHAINWORKS_OUTPUT
/chainworks_output.json
/tmp_chainworks_output.json
