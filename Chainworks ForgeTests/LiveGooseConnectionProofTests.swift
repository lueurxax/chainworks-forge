import Testing
@testable import Chainworks_Forge

// MARK: - LiveConnectionProofTests (removed)

/// This test suite has been removed along with the legacy transport types it
/// depended on. The file is retained as an empty stub so the Xcode project
/// reference stays valid.
@MainActor
@Suite("Live Connection", .tags(.live))
struct LiveConnectionProofTests {}
