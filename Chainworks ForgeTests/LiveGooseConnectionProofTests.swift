import Testing
@testable import Chainworks_Forge

// MARK: - LiveGooseConnectionProofTests (removed)

/// This test suite has been removed along with the GooseServerTransport and
/// GooseServerManager types it depended on. The file is retained as an empty
/// stub so the Xcode project reference stays valid.
@MainActor
@Suite("Live Goose Connection", .tags(.live))
struct LiveGooseConnectionProofTests {}
