{
    "mcpServers": {
        "chainworks-control-plane": {
            "url": "http://127.0.0.1:4000/mcp",
            "headers": {
                "Authorization": "Bearer ${CHAINWORKS_MCP_TOKEN}"
            }
        }
    }
}
