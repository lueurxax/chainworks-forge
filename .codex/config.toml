# Chainworks Forge repo-local Codex snippet
#
# Keep subagent orchestration shallow and predictable for this repository.
# Merge equivalent settings into your user-level `~/.codex/config.toml` if your
# Codex build supports project-level agent controls.
#
# Intended policy:
# max_depth = 1
# thread_limit = 3
# read_only_agents = [
#   "proposal_explorer",
#   "integration_mapper",
#   "ui_reviewer",
#   "ux_reviewer",
#   "arch_reviewer",
#   "product_reviewer",
#   "research_scout",
# ]
# baseline_refresh_mapper = "integration_mapper"
# baseline_runtime_operator = "xcode_operator"
# xcode_mcp_allowed_agents = ["xcode_operator"]
# proposal_reviews_collect_runtime_evidence = false
# research_collects_runtime_evidence = false
# baseline_refresh_validates_new_features = false
