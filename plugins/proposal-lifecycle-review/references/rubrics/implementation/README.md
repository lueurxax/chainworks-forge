# Implementation Rubrics

Implementation-audit rubrics are preserved at `skills/proposal-implementation-audit/references/rubrics/`.

The plugin keeps them skill-local to avoid breaking the implementation-audit registry's relative links.
